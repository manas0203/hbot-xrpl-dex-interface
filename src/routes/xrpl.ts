import express from 'express';
import { Request, Response } from 'express';
import {
  latency,
  reportConnectionError,
  statusMessages,
} from '../services/utils';
import { logger } from '../services/logger';


// Need to define all API endpoints here
// /balances
// /start
// /prices
// /trade

export default router;
